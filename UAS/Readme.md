### Pastikan semua database sudah terkoneksi dan terhubung 

``` 
jalankan perintah untuk melihat output
mysql -u root -p
USE PT_BahanBakuku;
SHOW DATABASES;
SELECT * FROM (nama_tabel);
```
#### untuk merestart server MySql 
- sudo systemctl start mysql
